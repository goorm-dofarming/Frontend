
REPOSITORY=/home/ubuntu/deploy

# 권한 부여
sudo chown -R ubuntu:ubuntu /home/ubuntu/deploy
sudo chmod -R 755 /home/ubuntu/deploy

# 특정 경로의 Directory 삭제
TARGET_DIR="/home/ubuntu/deploy/node_modules/.pnpm/next@14.2.4_react-dom@18.3.1_react@18.3.1__react@18.3.1_sass@1.77.8/node_modules/sass/types"

# 작업 디렉토리로 이동
cd $REPOSITORY

# Remove the specified target directory if it exists
if [ -d "$TARGET_DIR" ]; then
  echo "Removing conflicting directory: $TARGET_DIR"
  sudo rm -rf "$TARGET_DIR"
else
  echo "Directory does not exist: $TARGET_DIR"
fi